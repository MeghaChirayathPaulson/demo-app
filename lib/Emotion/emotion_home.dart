import 'package:flutter/material.dart';

class EmotionHome extends StatelessWidget {
  const EmotionHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Emotion Detector'),
      ),
    );
  }
}
